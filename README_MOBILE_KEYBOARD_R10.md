# Mobile keyboard fix r10

The composer is pinned to the current iOS visual viewport instead of resizing or moving the whole application layout. While the keyboard is open, the document is locked, the mobile dock is hidden, and only the message list is scrollable.
